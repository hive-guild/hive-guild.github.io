{
  "name": "hive-forever",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-auth.mjs && node scripts/build-pages.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --test tests/*.test.mjs && node scripts/validate-sql.mjs",
    "validate:sql": "node scripts/validate-sql.mjs"
  },
  "dependencies": {
    "@supabase/auth-js": "2.116.0"
  },
  "devDependencies": {
    "@electric-sql/pglite": "0.5.8",
    "esbuild": "0.25.10"
  }
}
